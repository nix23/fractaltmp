class MobileInputOverlay extends Component {
    render() {
        /* Old set __mobileInputOverlay Css:
               -> top: origInput.position().top;
               -> left: origInput.position().left;
               -> width: origInput.outerWidth();
               -> height: origInput.outerHeight();
           +Set origInput zIndex to 1;
        */
    }
};