// Was created when we thought to implement TREE-ALIKE forms.
// We have replaced them with distinct(separate) forms, so seems
// this can't be implemented(because we have no single root form 
// with N subforms -> we have a SET of N forms)